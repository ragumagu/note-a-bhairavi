# Underlines

Everything you put within a (pair of parenthesis) is underlined with (a single underline).

((This sentence should have two underlines under it)).

You can have (nested parenthesis (like this)).

You can escape underlining by putting two underscores before and 
after the parens, __(like this)__.

You can also have (nested parenthesis __(like this)__).

You can also have __(nested parenthesis (like this))__.

You can also have __(nested parenthesis __(like this)__)__.